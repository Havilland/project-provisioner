FROM registry.redhat.io/openshift4/ose-ansible-operator:v4.1

USER 0

RUN yum --disableplugin=fastestmirror -y install git && yum clean all

USER 1001

COPY watches.yaml ${HOME}/watches.yaml

COPY roles/ ${HOME}/roles/
